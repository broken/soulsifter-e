{
  "name": "soulsifter",
  "version": "1.0.2",
  "description": "back end for Soulsifter app",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "npm i",
    "generate_model": "ruby generate_model.rb",
    "rebuild": "HOME=~/.electron-gyp npx node-gyp rebuild --target=21.4.2 --arch=x64 --dist-url=https://atom.io/download/electron",
    "test": "npm run build && node test.js"
  },
  "author": "Robert Neale",
  "license": "Apache-2.0",
  "gypfile": true,
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^6.0.0"
  },
  "devDependencies": {
    "node-gyp": "^9.3.1"
  }
}
